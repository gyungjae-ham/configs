#!/bin/bash
SERENA_PATH=/Users/luca/Desktop/serena

claude mcp add serena --scope project -- uv run --directory $SERENA_PATH serena-mcp-server --context claude-code --project "$(pwd)"

echo "✅ Serena MCP가 $(pwd) 프로젝트에 추가되었습니다"

