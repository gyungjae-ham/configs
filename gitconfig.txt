[user]
    name = bind-luca
    email = luca@bindcorporation.com

[alias]
    l = log
    s = status
    a = add .
    hs = log --graph --all --pretty=format:'%C(yellow)[%ad]%C(reset) %C(green)[%h]%C(reset) | %C(white)%s %Cgreen(%cr) %C(bold blue){{%an}}%C(reset) %C(blue)%d%C(reset)' --date=short
    overview = "!f() { \
      echo \"=== Branch Information ===\"; \
      git branch -vv; \
      echo; \
      echo \"=== Status Overview ===\"; \
      git status -sb; \
      echo; \
      echo \"=== Modified Files ===\"; \
      git diff --name-status; \
      echo; \
      echo \"=== Staged Files ===\"; \
      git diff --name-status --staged; \
      echo; \
      echo \"=== Recent Commits ===\"; \
      git log --oneline -n 5; \
      echo; \
      echo \"=== Stashed Changes ===\"; \
      git stash list; \
    }; f"

[core]
    # 파일명 대소문자 구분 (Windows/macOS 혼용 시 발생할 수 있는 문제 방지)
    ignorecase = false
    # 파일명에 한글이 포함되어도 깨지지 않게 표시
    quotepath = false
    # 커밋 메시지 작성 시 기본 에디터 설정 (예: code --wait, vim, nano 등)
    # editor = code --wait

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[color "status"]
    added = green bold
    changed = yellow bold
    untracked = red bold
    branch = magenta bold

[column]
    ui = auto

[branch]
    sort = -committerdate

[tag]
    sort = version:refname

[init]
    defaultBranch = main

[diff]
    algorithm = histogram
    colorMoved = plain
    mnemonicPrefix = true
    renames = true

[push]
    default = simple
    autoSetupRemote = true
    followTags = true

[fetch]
    prune = true
    pruneTags = true
    all = true

[help]
    autocorrect = prompt

[commit]
    # 커밋 시 변경 사항(diff)을 에디터 하단에 보여줌 (실수 방지)
    verbose = true

[rerere]
    enabled = true
    # autoupdate = true (위험하여 제거: 자동 해결된 코드를 검토 없이 add하지 않도록 함)

[rebase]
    autoSquash = true
    autoStash = true
    updateRefs = true

[merge]
    # 충돌 발생 시 공통 조상 코드까지 보여주는 zdiff3 (매우 유용)
    conflictstyle = zdiff3

[pull]
    rebase = true
